# 开发环境 - 支持热重载
FROM node:20-alpine

WORKDIR /app

# 复制 package 文件
COPY package*.json ./
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动 Vite dev server
CMD ["npm", "run", "dev", "--", "--host"]
